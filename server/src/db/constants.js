const FIELDS = 'fields';
const TRACKING_TYPES = 'tracking_types';

const USERS = 'users';
const ROLES = 'roles';

const LOCATIONS = 'locations';
const COMPANIES = 'companies';
const COMPANY_LOCATIONS = 'company_locations';

const ACTIVITIES = 'activities';

const tableNames = {
  FIELDS,
  TRACKING_TYPES,

  USERS,
  ROLES,

  LOCATIONS,
  COMPANIES,

  COMPANY_LOCATIONS,

  ACTIVITIES,
};

module.exports = {
  tableNames,
};
